<?xml version="1.0" encoding="utf-8"?>
<!--
  Flavor-specific manifest for `prod` only — merged on top of the main
  manifest by Android's build-variant manifest merger (this is the
  standard app/src/<flavor>/AndroidManifest.xml mechanism, not a
  duplicate/competing manifest).

  Personal-use build: SMS capture is intentionally enabled in this flavor.
  Play Store policy review is deferred until the product behaviour is
  complete; this manifest must not silently disable the spending feature.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
</manifest>
